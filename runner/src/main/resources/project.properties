# suppress inspection "UnusedProperty" for whole file

# For some reason if this is not included we bomb out on loads
# this is probably due to the default annotation not working
# property in GuicyFig
manager.endpoint=http\://localhost\:8080/manager/text

# chop configuration parameters
chop.version=${project.version}
test.stop.timeout=500
sleep.to.stop=100
